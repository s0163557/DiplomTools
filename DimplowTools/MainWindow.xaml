<Window x:Class="DimplowTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DimplowTools"
        xmlns:viewModel="clr-namespace:DimplowTools.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1200">
    
    <Window.DataContext>
        <viewModel:MainWindowVM/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button 
            Width="150" 
            Height="100" 
            Content="Создать вершины"
            Command="{Binding GenerateGraphCommand}"/>



        <Grid Grid.Column="1" x:Name="CoordinateGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
        </Grid>

        <ItemsControl 
            Grid.Column="1" 
            ItemsSource="{Binding Vertices}" 
            x:Name="GraphCanvas">
            
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Width="10" Height="10" Fill="Black" 
                      Stroke="DarkGray" StrokeThickness="3"
                      Margin="{Binding PositionOnCanvas}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>

    </Grid>
</Window>
